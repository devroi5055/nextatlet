# Archive — historical session notes

These are **point-in-time notes** from the initial "Step 1" backend build and its bug-fix passes. They are **not current status** and are not maintained — the codebase has progressed well beyond them (auth, registration, action tokens, consent, control model, club registry, etc.).

For current, maintained documentation see:

- `CLAUDE.md` (repo root) — global context + implemented-vs-planned status
- `docs/` — the architecture spec
- `BACKEND_QUICK_START.md` — setup guide

Kept only for historical reference; safe to delete.
